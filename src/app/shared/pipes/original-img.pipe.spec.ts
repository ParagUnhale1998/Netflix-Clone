import { OriginalIMGPipe } from './original-img.pipe';

describe('OriginalIMGPipe', () => {
  it('create an instance', () => {
    const pipe = new OriginalIMGPipe();
    expect(pipe).toBeTruthy();
  });
});
